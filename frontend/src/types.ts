// Re-export shared types from project root
// This ensures frontend code can continue importing from './types'
// while using the shared contract defined in types/todo.types.ts
export * from '../../types/todo.types';
